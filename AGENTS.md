# AGENTS.md

Contract for all coding agents in this repository.

- Substantial work → `git wt <slug>`. Reach main only via `git land`.
- Never bare-push, never stash, never `git clean`/force without explicit approval.
- Read `CLAUDE.md` or `README.md` when present; repo-specific guides override this file.

