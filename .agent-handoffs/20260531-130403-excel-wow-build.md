## Session Handoff - Build Excel Wow Skill End To End

### Original Task
Build the `excel-wow` skill end to end in `/Users/danb/Desktop/skills`, turning the current placeholder into a production-ready FP&A / financial-modeling skill. The user wants it to be a flagship skill, not a generic Excel helper.

### Suggested Starting Point
Start with [excel-wow/SKILL.md](/Users/danb/Desktop/skills/excel-wow/SKILL.md). It is still a placeholder with `TBU` sections and no supporting resources. Build the skill around the user's edge: former banker/CFO, FP&A-grade operating models, model audit, formatting, scenario analysis, KPI bridges, and decision-compression outputs.

### Current Environment & Transfer Notes
- Current working directory: `/Users/danb/Desktop/skills`
- Current branch: check with `git branch --show-current`
- Current repo has unrelated active edits in other skills. Do not revert them.
- The scan found 16 skills after a new `bogdan-baciu-design` skill was added.
- Use the existing `skill-creator` standard: precise trigger metadata, concise body, progressive disclosure, deterministic scripts for fragile Excel operations, evals for realistic usage, and `agents/openai.yaml`.

### Current Dirty Worktree Before Excel Wow Work
These files were already modified/untracked during the audit and should be preserved:
- `handoff/SKILL.md`
- `handoff/evals/evals.json`
- `pagecraft/skills/number-formats/SKILL.md`
- `pagecraft/skills/number-formats/apply-number-formats.py`
- `pagecraft/skills/reskin/reskin.py`
- `pagecraft/skills/verify-text-wrap/runner.py`
- `skill-drift-scanner/SKILL.md`
- `weekly-update/SKILL.md`
- `pagecraft/skills/number-formats/evals/`
- `pagecraft/skills/reskin/tests/`
- `pagecraft/skills/verify-text-wrap/agents/`

### What Was Verified In This Session
- `frontmatter ok: 16 skills`
- `json-ok`
- `python syntax ok: 9 files`
- `pagecraft/skills/number-formats/evals/workbook_fixture_eval.py` passed
- `python3 -m unittest pagecraft/skills/reskin/tests/test_reskin.py` passed: 7 tests
- `python3 pagecraft/skills/verify-text-wrap/runner.py --help` now returns quickly
- `sh sync-skills.sh --all --dry-run` completed and includes `excel-wow`

### What's Left
- [ ] Replace [excel-wow/SKILL.md](/Users/danb/Desktop/skills/excel-wow/SKILL.md) with a real skill. It should trigger on financial model build/review/audit/cleanup, operating model, budget/forecast model, KPI bridge, variance analysis, scenario/sensitivity model, Excel formula review, and "make this model banker-grade".
- [ ] Define the boundary clearly: `excel-wow` builds and audits workbook logic, layout, operating-model structure, and decision outputs. `number-formats` remains the formatting standard for Macabacus-style number formats and colors.
- [ ] Add `excel-wow/agents/openai.yaml` with a concise display name, short description, and default prompt.
- [ ] Add `excel-wow/references/modeling-standards.md` covering sheet order, model architecture, assumptions/drivers, historicals, forecast, checks, scenarios, outputs, row/column conventions, formula discipline, and audit expectations.
- [ ] Add `excel-wow/references/model-review-checklist.md` covering formula consistency, hardcodes in formulas, circularity, hidden rows/columns, external links, stale named ranges, broken formulas, balance checks, variance checks, and presentation readiness.
- [ ] Add `excel-wow/scripts/inspect_workbook.py` using `openpyxl` to produce a workbook audit report: sheets, dimensions, formulas, external links, merged cells, hidden sheets/rows/columns, blank rows/columns, formula errors, volatile functions, suspicious hardcodes, and style/provenance summary.
- [ ] Add `excel-wow/scripts/build_model_skeleton.py` or equivalent only if it removes real repeated work. It should create a clean workbook skeleton with sheets such as `Cover`, `Inputs`, `Historicals`, `Forecast`, `Scenarios`, `Checks`, and `Outputs`.
- [ ] Reuse `pagecraft/skills/number-formats/formats.json` or call `pagecraft/skills/number-formats/apply-number-formats.py`; do not duplicate Macabacus codes in two places.
- [ ] Add `excel-wow/evals/workbook_audit_fixture_eval.py` that creates a small workbook with intentional issues, runs the inspector, and asserts that the expected issues are found.
- [ ] Add 2-3 example prompts directly in `SKILL.md`: model audit, model cleanup, and build-from-CSV/assumptions.
- [ ] Add output contract: summary, priority findings, workbook changes made, formulas/checks added, files written, commands run, and residual risks.
- [ ] Validate with:
  ```bash
  ruby -e 'require "yaml"; Dir.glob("**/SKILL.md").each{|f| t=File.read(f); fm=t[/\A---\n(.*?)\n---\n/m,1] or abort("missing frontmatter #{f}"); y=YAML.safe_load(fm); abort("missing name #{f}") unless y["name"]; abort("missing desc #{f}") unless y["description"]}; puts "frontmatter ok"'
  find . -name '*.json' -not -path './.git/*' -not -path '*/__pycache__/*' -print0 | xargs -0 -I{} python3 -m json.tool {} >/dev/null
  PYTHONDONTWRITEBYTECODE=1 python3 excel-wow/evals/workbook_audit_fixture_eval.py
  sh sync-skills.sh --all --dry-run
  git diff --check
  ```

### Key Decisions
- Treat `excel-wow` as a modeling/audit skill, not a formatting-only skill.
- Keep the `SKILL.md` concise and put detailed standards/checklists in `references/`.
- Use scripts for fragile workbook inspection because Excel audits are easy to under-specify in prose.
- Preserve all unrelated dirty worktree changes.

### Acceptance Criteria
- No `TBU` placeholder remains in `excel-wow/SKILL.md`.
- The skill has `SKILL.md`, `agents/openai.yaml`, at least one reference file, at least one deterministic workbook script, and at least one fixture eval.
- The trigger metadata is specific enough to invoke on real FP&A/modeling requests and avoid generic spreadsheet cleanup.
- The eval proves the inspector catches intentional workbook issues.
- The final report includes exact commands run and pass/fail status.
