interface:
  display_name: "Parallel Dispatch"
  short_description: "Split work into safe tracks or code-wins variants."
  default_prompt: "Use $parallel-dispatch to convert this work split or code-wins variant decision into independent agent prompts plus a coordinator playbook with evidence, file scopes, dependencies, and merge order."
