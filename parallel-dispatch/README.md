# parallel-dispatch

## What it does
Formats a user-defined parallel plan into copy-paste prompts for multiple agents and a coordinator playbook.

## Install
Use the install command generated by your skills registry after publishing this repository.

## Quickstart
Prompt: `generate parallel dispatch prompts for these 4 tracks`
